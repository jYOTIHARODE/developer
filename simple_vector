#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int> arr; 
    for (int i = 1; i <= n; i++) 
    {
        int x;
        cin>>x;
        arr.push_back(x);
    }
     for (auto it = arr.begin(); it != arr.end(); it++) 
        cout << *it<< " "; 
    return 0;
}
