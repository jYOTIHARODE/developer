#include<iostream.h>
long fact(int n)
{
  if(n==0)
    return 1;
   return(n*fact(n-1));
}
int main()
{
  int num;
  cout<<"Enter a positive integer<<endl;
  cin>>num;
  cout<<"factorial of given number="<<fact(num);
  return 0;
}
